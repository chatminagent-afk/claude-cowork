# VIRA V3.3 — Changelog (21 Juni 2026)

Basis: `2026-06-21-VIRA-V3.2.json`. File baru: `2026-06-21-VIRA-V3.3.json` (55 node, JSON valid).

## Root cause: link GForm tidak terkirim (device salah)
Di hasil UAT, `Process All` sudah benar (`isSendGForm: true`, `gformLinkName: "GForm Pendaftaran Batch 5"`), tapi link tetap tidak sampai di chat.

Penyebab: **`Send GForm Link` (dan klon-nya `Send GForm Clarify`) memakai `device_id: D-GDNA8`**, sementara SEMUA node kirim lain + webhook masuk memakai **`D-4ZV1F`**. Jadi balasan utama terkirim (D-4ZV1F) tapi pesan link dikirim lewat device D-GDNA8 yang tidak aktif → gagal/tidak sampai. `D-GDNA8` ini warisan dari V3.1.

## Perubahan
1. **Fix device pengirim** — `device_id` di `Send GForm Link` & `Send GForm Clarify`: `D-GDNA8` → `D-4ZV1F`. Sekarang ke-7 node Kirimi seragam pakai `D-4ZV1F`.
2. **Whitelist** — tambah `628176480658` ke node `IF (Whitelist)` (combinator OR). Daftar sekarang: 6285155202354, 6281510624599, 6596110395, 6589200455, 628176480658.

## Validasi
- JSON valid, 55 node, tidak ada lagi `D-GDNA8` di workflow.
- Semua node httpRequest Kirimi = `device_id: D-4ZV1F`.

## Action item
- Re-import `2026-06-21-VIRA-V3.3.json` (timpa V3.2 yang barusan diimpor).
- UAT ulang dgn 628176480658: kirim "daftar dmn ya" → harus terima balasan utama + pesan link Batch 5 terpisah.
- Catatan: kalau ke depan ganti device WhatsApp, update `device_id` di semua node Kirimi sekaligus.
