# VIRA V3.1 — Handoff sesi 21 Juni 2026 (untuk agent berikutnya)
Proyek: VIRA AI Chatbot "Sam" (The Scholars) di n8n · WhatsApp via Kirimi · Model **Claude Haiku 4.5**.
Folder kerja: `D:\Documents\Claude Cowork\the scholars\enhancement boros v2 vira 18 juni`.

## 1. Konteks masuk sesi ini
- User sudah punya **V3** (`2026-06-19-VIRA-V3.json`) hasil sesi sebelumnya (Haiku, tanpa tool agent, data di-inject dari tab PROGRAM sebagai single source of truth, memory sessionKey=`user_wa` tanpa getHours).
- V3 **belum** diimplementasi live di awal sesi. Sam mengirim **report baru (20 Juni)** = 3 chat gagal yang masih jalan di V2.
- Permintaan user: cek apakah V3 sudah menyelesaikan masalah report 20 Juni; kalau belum, beri penyesuaian; dan putuskan tetap Haiku atau pindah Sonnet.

## 2. 3 chat gagal report 20 Juni (di `after implementing v2/report 20 juni`)
1. **812-9906-625** — anak naik kelas 9, bot benar arahkan Intermediate TAPI ikut menawarkan Seniors/mentoring kelas 12 yang tak diminta → kurang disiplin segmentasi.
2. **813-7900-9567** — jeda 6 hari, user balas "🙏", bot reset & tanya ulang "ortu atau murid" → continuity/memory.
3. **821-1633-2737** — user mahasiswa semester 6 (di luar scope), bot terus tanya "SMP atau SMA?" lalu mengulang saat user balas "inii" → out-of-scope tak ditangani + loop, chat mati.

## 3. Analisis & keputusan
- **Tetap Haiku 4.5.** Dari 3 masalah: #1 & #2 = arsitektur/prompt (Sonnet tidak memperbaiki; #2 murni memory). #3 menyentuh kapabilitas tapi ditambal guardrail deterministik + handoff. Sonnet wholesale = mahal & tidak menyelesaikan #2.
- Tentang memory: V3 sudah fix reset per-jam, dan tab **STATS** sudah mem-persist `user_status`+`greeting_sent` per user (parent/student durable lintas hari). Yang volatile hanya `Simple Memory` LLM (RAM, 10 turn).
- **User memilih fix #2 secara prompt-only** (tidak menambah kolom Google Sheet).

## 4. Yang sudah dibangun: VIRA_V3.1 (file final = `2026-06-21-VIRA-V3.1.json`)
Dibuat dari V3 (V3 TIDAK ditimpa). 52 node, JSON valid, 0 tool agent, syntax JS Preprocess & Process All OK.

Perubahan:
1. **#1 Segmentasi program** (prompt `# REKOMENDASI PROGRAM` + Preprocess): sebut HANYA program sesuai kelas anak; jangan tawarkan tier lain kecuali ditanya.
2. **#2 No re-greet returning user** (prompt `# STATUS`, prompt-only): status sudah ada / greeting sudah terkirim → jangan ulang sapaan pembuka; balasan singkat ("ok","🙏") jangan mereset chat.
3. **#3 Out-of-scope + anti-loop** (Preprocess + prompt `# ALUR` 2b/2c): Preprocess deteksi `6b outOfScope` (mahasiswa/semester N/kuliah/sarjana/kerja/lulus dst — sengaja TIDAK pakai kata "kuliah" telanjang agar "anak mau kuliah di SG" tak salah-flag) dan `6c unclearReply` (balasan ≤4 char spt "inii"); prompt: user di luar jenjang → jujur tidak ada program, jangan tanya kelas; klarifikasi maks 1x → `[TALK_TO_SAM]`.
4. **Fix tag bocor** (ketemu saat user uji V3 live): bot kirim `[USERSTATUS:UNKNOWN] Halo!...`. Akar: stripper `Process All` terlalu ketat `/\[USER_STATUS:(PARENT|STUDENT)\]/g`. Fix → strip toleran `/\[\s*USER[_\s]?STATUS\s*:\s*[^\]]*\]/gi` (tangkap tanpa underscore + nilai apa pun) + prompt: nilai tag hanya PARENT/STUDENT, kalau belum tahu status jangan pasang tag. Tested 5 varian, bersih.

## 5. ⚠️ Action item sebelum/saery publish
- **Re-import `2026-06-21-VIRA-V3.1.json`** — V3 live yang sekarang dipakai user masih punya bug tag bocor + 3 gap di atas.
- **Harga Mock Interview di tab PROGRAM** masih `to be confirmed by Sam` → ubah jadi **Rp 750.000** (sesuai HARGA/MOCK_INTERVIEW). Bot hanya baca PROGRAM. User menyebut sudah merge batch+harga ke PROGRAM — pastikan sel ini ikut benar.

## 6. Belum dilakukan / next steps
- **Belum UAT live.** Skenario: (a) parent anak kelas 9 → Intermediate saja tanpa Seniors; (b) returning user balas "🙏" → tidak ditanya ulang status; (c) mahasiswa semester 6 → jujur tidak ada program, tidak loop, tawarkan Sam saat balasan ambigu; (d) cek tidak ada lagi tag bocor.
- **Open question ke user (belum dijawab):** apakah tag bocor muncul konsisten atau hanya sekali? Apakah ada tag lain yang pernah bocor (`[SEND_GFORM]`,`[UNKNOWN]`)? Kalau iya → buat semua stripper toleran.
- **Sisa risiko #2 yang disadari (karena prompt-only):** detail kelas/program anak tidak dipersist ke sheet → setelah jeda sangat panjang + restart n8n, bot bisa tanya ulang kelas. Solusi penuh = tambah 3 kolom STATS (`grade`,`recommended_program`,`gform_sent`) + node read/write. Belum dikerjakan (menunggu keputusan user).
- Opsi jangka panjang: hybrid routing Haiku→Sonnet hanya saat `[UNKNOWN]`/multi-intent/low-confidence; regression eval set dari chat gagal.

## 7. File terkait
- `2026-06-21-VIRA-V3.1.json` — workflow final (import ini).
- `2026-06-21-VIRA-V3.1-changelog.md` — detail perubahan + validasi.
- `2026-06-21-VIRA-V3.1-handoff.md` — file ini.
- Basis: `2026-06-19-VIRA-V3.json` + `2026-06-19-VIRA-V3-handoff.md`/`-changelog.md`.
- Analisis akar masalah V2: `2026-06-19-VIRA-V2-analisis-root-cause-ID.md`.
- Report 20 Juni: `after implementing v2/report 20 juni/*.zip`.
- Database: `The_Scholars_Database.xlsx` (tab PROGRAM, STATS, HARGA, dst).

## 8. Catatan arsitektur penting (jangan diulang salah)
- Tidak ada tool agent — semua fakta di-inject via node `FAQ Retrieve` (data_context+faq_context) dari read PROGRAM/ABOUT/LINKS/FAQ. Jangan tambah tool.
- Single source of truth = tab **PROGRAM**. Jangan pecah ke BATCH/HARGA/SYARAT terpisah.
- Tag kontrol (`[USER_STATUS:..]`,`[SEND_GFORM]`,`[UNKNOWN]`,`[TALK_TO_SAM]`,`[MOCK_INTERVIEW_BOOKING]`) dibersihkan di node `Process All` sebelum dikirim ke WhatsApp.
- Kredensial sama dengan instance n8n user → auto-resolve setelah import.
